package com.conference.tickets.group;

public class Premium {

    // Enable the line below to break the test that asserts that classes under the
    // "tickets" package should not depend on each other. i.e: group and singular
    // packages should be free of cyclic dependencies
//    private FiveDay fiveDayTickets = new FiveDay();
}
