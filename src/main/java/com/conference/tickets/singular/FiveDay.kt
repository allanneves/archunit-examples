package com.conference.tickets.singular

import java.time.LocalDateTime

class FiveDay {

    fun checkAvailabilityForDate(date: LocalDateTime = LocalDateTime.now()) : Boolean {
        // Enable the line below to break the test that checks for the usage of System.out.println
        // println(date)
        return false;
    }
}
